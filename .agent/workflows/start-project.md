---
description: Start all Kubernetes workloads for this project
---
// turbo
1. Scale up deployments and statefulsets to 1 in all project namespaces
```bash
./manage-project.sh start
```
2. Check the status of the workloads
```bash
./manage-project.sh status
```
