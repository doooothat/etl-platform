apiVersion: "sparkoperator.k8s.io/v1beta2"
kind: SparkApplication
metadata:
  name: iceberg-nessie-restore
  namespace: spark
spec:
  type: Python
  pythonVersion: "3"
  mode: cluster
  image: "apache/spark:3.5.4"
  imagePullPolicy: IfNotPresent
  mainApplicationFile: "local:///tmp/script/restore_data.py"
  sparkVersion: "3.5.4"
  restartPolicy:
    type: Never
  volumes:
    - name: script-vol
      configMap:
        name: spark-restore-script
    - name: tmp-vol
      emptyDir: {}
  driver:
    cores: 1
    memory: "1g"
    labels:
      version: 3.5.4
    serviceAccount: spark-operator-sa
    env:
      - name: AWS_REGION
        value: us-east-1
    initContainers:
      - name: setup-ivy
        image: busybox
        command:
          ["sh", "-c", "mkdir -p /tmp/cache /tmp/jars && chmod -R 777 /tmp"]
        volumeMounts:
          - name: tmp-vol
            mountPath: /tmp
    volumeMounts:
      - name: script-vol
        mountPath: /tmp/script
      - name: tmp-vol
        mountPath: /tmp
  executor:
    cores: 1
    instances: 1
    memory: "1g"
    initContainers:
      - name: setup-ivy
        image: busybox
        command:
          ["sh", "-c", "mkdir -p /tmp/cache /tmp/jars && chmod -R 777 /tmp"]
        volumeMounts:
          - name: tmp-vol
            mountPath: /tmp
    volumeMounts:
      - name: script-vol
        mountPath: /tmp/script
      - name: tmp-vol
        mountPath: /tmp
  sparkConf:
    "spark.jars.packages": "org.apache.iceberg:iceberg-spark-runtime-3.5_2.12:1.7.1,org.projectnessie.nessie-integrations:nessie-spark-extensions-3.5_2.12:0.101.3,org.apache.iceberg:iceberg-aws-bundle:1.7.1"
    "spark.jars.ivy": "/tmp"
    "spark.sql.extensions": "org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions,org.projectnessie.spark.extensions.NessieSparkSessionExtensions"
    "spark.sql.catalog.nessie": "org.apache.iceberg.spark.SparkCatalog"
    "spark.sql.catalog.nessie.uri": "http://nessie.nessie.svc.cluster.local:19120/api/v1"
    "spark.sql.catalog.nessie.ref": "main"
    "spark.sql.catalog.nessie.warehouse": "s3://iceberg-data/"
    "spark.sql.catalog.nessie.catalog-impl": "org.apache.iceberg.nessie.NessieCatalog"
    "spark.sql.catalog.nessie.io-impl": "org.apache.iceberg.aws.s3.S3FileIO"
    "spark.hadoop.fs.s3a.endpoint": "http://minio.minio.svc.cluster.local:9000"
    "spark.hadoop.fs.s3a.access.key": "admin"
    "spark.hadoop.fs.s3a.secret.key": "password"
    "spark.hadoop.fs.s3a.path.style.access": "true"
    "spark.hadoop.fs.s3a.impl": "org.apache.hadoop.fs.s3a.S3AFileSystem"
    "spark.hadoop.fs.s3a.aws.credentials.provider": "org.apache.hadoop.fs.s3a.SimpleAWSCredentialsProvider"
    "spark.sql.catalog.nessie.client.region": "us-east-1"
    "spark.sql.catalog.nessie.s3.endpoint": "http://minio.minio.svc.cluster.local:9000"
    "spark.sql.catalog.nessie.s3.path-style-access": "true"
    "spark.sql.catalog.nessie.s3.access-key-id": "admin"
    "spark.sql.catalog.nessie.s3.secret-access-key": "password"
    "spark.sql.catalog.nessie.s3.region": "us-east-1"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: spark-restore-script
  namespace: spark
data:
  restore_data.py: |
    from pyspark.sql import SparkSession
    import sys

    spark = SparkSession.builder.getOrCreate()

    print("--- Starting Iceberg + Nessie Standard Test ---")

    # 1. Create Data in Main
    print("Step 1: Creating data in main branch...")
    data_main = [("1", "Alice", 30), ("2", "Bob", 25)]
    df_main = spark.createDataFrame(data_main, ["id", "name", "age"])
    spark.sql("CREATE DATABASE IF NOT EXISTS nessie.db")
    df_main.writeTo("nessie.db.users").createOrReplace()

    print("Table in 'main':")
    spark.table("nessie.db.users").show()

    # 2. Branching Test
    print("Step 2: Creating branch 'experiment' and adding data...")
    spark.sql("CREATE BRANCH experiment IN nessie FROM main")
    spark.sql("USE REFERENCE experiment IN nessie")

    new_data = [("3", "Charlie", 35)]
    df_new = spark.createDataFrame(new_data, ["id", "name", "age"])
    df_new.writeTo("nessie.db.users").append()

    print("Table in 'experiment':")
    spark.table("nessie.db.users").show()

    # 3. Verify Isolation
    print("Step 3: Switching back to 'main' and verifying isolation...")
    spark.sql("USE REFERENCE main IN nessie")
    print("Table in 'main' (expected: 2 rows):")
    spark.table("nessie.db.users").show()

    count = spark.table("nessie.db.users").count()
    if count == 2:
        print("✅ SUCCESS: Nessie Integration Verified.")
    else:
        print(f"❌ FAILURE: Isolation failed. Found {count} rows.")
        sys.exit(1)

    print("--- Standard Test Completed ---")
    spark.stop()
